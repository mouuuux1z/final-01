# Server
PORT=3000
NODE_ENV=development

# Database (SQLite — local development, matches prisma/schema.prisma)
DATABASE_URL="postgresql://neondb_owner:npg_0mkEoU7OJSIa@ep-patient-wave-atyzhtxb-pooler.c-9.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
"

# PostgreSQL (use with docker-compose.yml — change schema provider to postgresql first)
# DATABASE_URL="postgresql://mydoc:mydoc_secret@localhost:5432/mydoc_db?schema=public"

# JWT (minimum 32 characters)
JWT_SECRET=mydoc-local-dev-jwt-secret-key-2026-secure
JWT_EXPIRES_IN=7d

# Uploads (relative to backend folder)
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880
